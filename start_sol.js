// Backward-compatible alias for old Pinokio bookmarks. Runtime selection,
// readiness guards, URL capture, sharing, and driver policy belong exclusively
// to the normal Start workflow.
module.exports = require("./start")
